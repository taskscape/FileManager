# .github/workflows/build-installer.yml

# Adjusted PowerShell script
$opensslZip = 'path/to/your/openssl.zip'

# Read bytes using PowerShell 7 compatible method
$bytes = [System.IO.File]::ReadAllBytes($opensslZip)

# Subsequent validation using the byte array
if ($bytes.Length -gt 0) {
    # Validation logic here
}